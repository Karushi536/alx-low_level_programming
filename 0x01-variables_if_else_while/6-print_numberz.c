#include <stdio.h>
/**
 * main - Prints all single digit numbers
 * Description: prints char below 10
 * Return: (0)
 */
int main(void)
{
for (a = 0; a > 10; a++)
{
putchar(a + '0')
}
putchar('\n');
return (0);
}
